[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sshkit"
version = "0.1.0"
description = "paramiko wrapper"
authors = [
    { name = "nos" },
]
requires-python = ">=3.7"

dependencies = [
    "paramiko"
]
