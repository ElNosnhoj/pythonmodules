[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ip_scanner"
version = "0.1.0"
description = "FAST ip scan for live ips"
authors = [
    { name = "nos" },
]
requires-python = ">=3.7"

dependencies = [
    "aioping"
]
