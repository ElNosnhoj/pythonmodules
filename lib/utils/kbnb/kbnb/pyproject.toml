[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kbnb"
version = "0.1.1"
description = "Non blocking input"
authors = [
    { name = "nos" }
]
requires-python = ">=3.7"

dependencies = []
