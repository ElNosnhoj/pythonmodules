[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "asyncdec"
version = "0.1.0"
description = "Async decorator tools"
authors = [
    { name = "nos" }
]
requires-python = ">=3.7"

dependencies = []
